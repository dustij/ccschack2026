// Stub for the `server-only` package in test environments.
// The real package throws at import time outside Next.js server context;
// this empty module lets Vite resolve the import without errors.
export {};
